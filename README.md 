lemuen/
├── src/
│ ├── main.c → Entry point
│ ├── parser.c/.h → แยก string คำสั่ง
│ ├── executor.c/.h → จัดการ exec/fork
│ ├── builtins.c/.h → คำสั่งภายใน (cd, exit)
│ ├── utils.c/.h → ฟังก์ชันช่วยเหลือ
├── include/ → เก็บ header
├── Makefile
└── README.md

พิมพ์คำสั่งใดก็ได้ เช่น ls, pwd, cat

เจอร์ใหม่ใน Lemuen v0.2:
รองรับ input redirection: ใช้ < เช่น cat < file.txt
รองรับ output redirection: > และ >> เช่น echo Hello > out.txt
แยกคำสั่ง, arguments และ redirection อย่างยืดหยุ่น
ใช้ dup2 + open แบบ raw syscall เหมือน shell จริง